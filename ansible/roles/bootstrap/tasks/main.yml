---

#
# bootstrap packages required for ansible to work
#

  - name: check if bootstrap is required
    local_action: stat path="{{ template }}"
    register: has_template

  - name: include os-specific tasks
    when: has_template.stat.exists
    include: "{{ template }}"
